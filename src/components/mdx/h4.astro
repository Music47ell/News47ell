---
const { id, ...attrs } = Astro.props
---

<h4 id={id} class="md:text-lg font-bold tracking-wide text-gray-100" {...attrs}>
	<a
		href={`#${id}`}
		class="text-red-500 hover:decoration-yellow-500 transition-colors duration-100 hover:underline"
		>#</a
	>
	<slot />
</h4>
